CLICKHOUSE_HOST=
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=

# --- Email Alerting Configuration ---
# Leave these blank to use "Development Mode" (Ethereal Email), 
# which prints a fake email preview link to the console instead of sending.

# The SMTP Server details (e.g., smtp.gmail.com)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Who should receive the alerts? (Defaults to admin@yourdomain.com if blank)
ALERT_EMAIL_RECIPIENT=admin@example.com